import 'package:flutter/material.dart';

/// Displays a fixed text message.
///
/// This custom component is here to help test the dart doc generation for
/// this library.
///
/// Example usage:
///
/// ```dart
/// CustomComponent();
/// ```
class CustomComponent extends StatelessWidget {
  const CustomComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('This is my custom component');
  }
}
